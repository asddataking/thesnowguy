/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['example.com'],
    },
    output: 'standalone',
  };
  
  export default nextConfig;
  



